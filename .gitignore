# ===== 敏感配置（含 API Key，切勿入库）=====
.env
# 备份/临时副本同样是明文密钥：.env 只能精确匹配一行，写了 .env.bak 或 .env.old
# 就变成未忽略文件了。统一按前缀忽略。
.env.*
!.env.example

# ===== 运行时用户数据（按需生成，不入库）=====
uploads/
translation_cache.json

# ===== 标签数据库（由 build_tag_db.py 构建/增量更新，体积大不入库）=====
data/

dump/

# ===== WD14 模型（体积大，按 WD14_MODEL_PATH 单独下载到 models/）=====
models/

# ===== Python 构建产物 =====
__pycache__/
*.pyc
.venv/
venv/

# ===== 日志 =====
*.log
logs/

# ===== 临时/回归测试产物 =====
nul
_*.py
_*.json

# ===== IDE 本地配置 =====
.claude/

# ===== AI 内部文档（不入库）=====
# 执行 git rm --cached CLAUDE.md 后此规则才真正生效（对已跟踪文件无效）。
# 注意：只停止后续跟踪，历史提交里的内容仍在（git log 与 GitHub 提交记录可查）。
# 若要连历史一起清除，需要改写历史（git filter-repo），会变更所有提交哈希。
CLAUDE.md
