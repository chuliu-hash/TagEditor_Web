# ===== 敏感配置（含 API Key，切勿入库）=====
.env
# 备份/临时副本同样是明文密钥：.env 只能精确匹配一行，写了 .env.bak 或 .env.old
# 就变成未忽略文件了。统一按前缀忽略。
.env.*
!.env.example

# ===== 运行时用户数据（按需生成，不入库）=====
uploads/
translation_cache.json

# ===== 标签数据库（由 build_tag_db.py 构建/增量更新，体积大不入库）=====
data/

dump/

# ===== WD14 模型（体积大，按 WD14_MODEL_PATH 单独下载到 models/）=====
models/

# ===== Python 构建产物 =====
__pycache__/
*.pyc
.venv/
venv/

# ===== 日志 =====
*.log
logs/

# ===== 临时/回归测试产物 =====
nul
_*.py
_*.json

# ===== IDE 本地配置 =====
.claude/

# 注：CLAUDE.md 曾在此处被列为忽略，但它早已被 git 跟踪（有一笔专门的提交），
# 而忽略规则对已跟踪文件无效——该行从未生效，只会造成「以为没入库」的误解。
# 已删除。若确实希望停止跟踪：git rm --cached CLAUDE.md
